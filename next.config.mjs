/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.pexels.com','i.pinimg.com','encrypted-tbn0.gstatic.com'],
      },
};

export default nextConfig;
